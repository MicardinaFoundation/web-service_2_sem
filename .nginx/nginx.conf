server {
    listen 80 default_server;

    location / {
        root /var/www/html;
        try_files $uri $uri /index.html =404;
    }

    location /api/ {
        proxy_pass http://student-app-api:10280;
    }
}